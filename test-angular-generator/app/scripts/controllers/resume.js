'use strict';

/**
 * @ngdoc function
 * @name testAngularGeneratorApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the testAngularGeneratorApp
 */
angular.module('alexlapinski')
  .controller('ResumeController', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate' // Resume Details / References
    ];
  });
